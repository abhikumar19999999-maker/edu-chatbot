import React from "react";
export default function Dashboard({data}){if(!data)return <div className="panel"><h2>Student Dashboard</h2><p>Loading analytics…</p></div>;const s=data.stats;const cards=[["Conversations",s.totalConversations,"💬"],["Questions",s.questionsAsked,"❓"],["Bot responses",s.botResponses,"🤖"],["Average rating",s.averageRating,"⭐"],["Helpful answers",s.helpfulAnswers,"👍"],["Subjects studied",s.subjectsStudied,"📚"]];return <section className="dashboard-view"><div className="stat-grid">{cards.map(x=><div className="stat-card" key={x[0]}><span>{x[2]}</span><strong>{x[1]}</strong><small>{x[0]}</small></div>)}</div><div className="dashboard-grid"><div className="panel"><h3>Subjects studied</h3>{data.subjects?.length?data.subjects.map(x=><div className="list-row" key={x.id}><span>{x.icon}</span><strong>{x.name}</strong></div>):<p className="muted">No subjects studied yet.</p>}</div><div className="panel"><h3>Recent conversations</h3>{data.recentConversations?.length?data.recentConversations.map(x=><div className="list-row" key={x.id}><div><strong>{x.title}</strong><small>Learning session</small></div><span>→</span></div>):<p className="muted">No conversations yet.</p>}</div></div></section>}
